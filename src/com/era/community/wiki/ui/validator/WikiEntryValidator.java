package com.era.community.wiki.ui.validator; 


public class WikiEntryValidator extends com.era.community.wiki.dao.generated.WikiEntryValidator 
{
	/*public String validateExternalURL(Object value) throws Exception
	{
		UrlValidator urlValidator = new UrlValidator( UrlValidator.NO_FRAGMENTS );       
		if (value.toString().length() > 0 && !urlValidator.isValid( value.toString())){
			return "The external URL above is not valid";
		}
		return null;
	}*/
}